spring:
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/cms?useSSL=false
    initialization-mode: always
  data:
    rest:
      base-path: /api
server:
  port: 8081
